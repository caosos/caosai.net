# Frontend

This directory will hold the React application rebuilt from `caosos/emergent-caos-build/frontend`.

## Initial source references
- `frontend/package.json`
- `frontend/craco.config.js`
- `frontend/src/...`

## First migration goal
Preserve the CAOS UI surface while removing Emergent-only dev tooling and keeping the app deployable on Ubuntu/Linode.
